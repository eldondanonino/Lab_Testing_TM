export function parseLine(line: string): string[] | null {
  return [];
}

function parseAddLine(line: string): string[] {
  return [];
}

function parseRemoveLine(line: string): string[] {
  return [];
}

function parseSetStateLine(line: string): string[] {
  return [];
}